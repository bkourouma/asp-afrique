{
  "repository": "@tonOrganisation/tonDepot",
  "personalAccessToken": "TON_TOKEN_SPECIFY",
  "rules": [
    {
      "name": "Extract all tokens to JSON",
      "path": "./output/all-tokens.json",
      "parsers": ["@specifyapp/parsers:to-json"]
    }
  ]
}
